# =========================================================
# 연구자 1 · 연구자 2 공통 의존성 (버전 완전 고정)
# model.pkl 을 만든 scikit-learn 버전과 불러오는 버전이
# 다르면 InconsistentVersionWarning / 로드 실패가 발생하므로
# 두 연구자가 반드시 동일한 버전을 사용해야 한다.
# =========================================================
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.0.2
joblib==1.4.2
