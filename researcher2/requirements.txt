# =========================================================
# 연구자 2 의존성
# 앞의 네 줄은 연구자 1 requirements.txt 와 "완전히 동일한 버전".
# → 동일 scikit-learn 버전으로 model.pkl 파일 로드해야 안전.
# 마지막 줄만 추론 작업용 Jupyter 환경 추가.
# =========================================================
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.0.2
joblib==1.4.2
jupyterlab==4.2.5